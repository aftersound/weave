package io.aftersound.weave.service.metadata;

import io.aftersound.weave.metadata.Control;
import io.aftersound.weave.service.ServiceExecutor;

/**
 * Conceptual entity represents execution {@link Control} for paring {@link ServiceExecutor}.
 * An {@link ExecutionControl} controls the behavior of pairing {@link ServiceExecutor}
 */
public interface ExecutionControl extends Control {
}
