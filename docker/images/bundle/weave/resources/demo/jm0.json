{
    "description": "This demo is about job manager",
    "covers": [
        "Register/Unregister job runner instance",
        "Receive heartbeat from job runner instance"
    ],
    "step1": {
        "description": "import Postman environments and collections by raw content into Postman. To get raw content of each collection or environment, run shell commands",
        "shellCommands": [
            "curl -H 'Accept: application/json' http://localhost:8080/demo/postman/environment/jm0",
            "curl -H 'Accept: application/json' http://localhost:8080/demo/postman/collection/jm0-rcm",
            "curl -H 'Accept: application/json' http://localhost:8080/demo/postman/collection/jm0-services"
        ]
    },
    "step2": {
        "description": "try the following services under Postman Collection 'jm0: runtime config management (use Environment mp0@8080)'",
        "services": [
            "CREATE Application",
            "GET Application",
            "CREATE Runtime Spec",
            "GET Runtime Spec"
        ],
        "explanation": "In essence, the runtime spec is the instruction for any service instance in namespace 'demo' and application 'jm0' to materialize/realize services as instructed."
    },
    "step3": {
        "description": "try to start a service instance that hosts services of job manager, which is expected to start successfully",
        "shellCommands": [
            "docker container run -it -p 8081:8080 --env WEAVE_PROFILE=standard --env WEAVE_NAMESPACE=demo --env WEAVE_APPLICATION=jm0 --env WEAVE_SERVICE_AGENT_ENABLED=true --env WEAVE_SERVICE_MANAGER=http://<YOUR_MACHINE_IP>:8080 aftersound/weave:bundle"
        ]
    },
    "step4": {
        "description": "try the services under Postman Collection 'jm0: services (use Environment jm0@8081)'"
    }
}